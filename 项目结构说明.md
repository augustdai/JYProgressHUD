# JYProgressHUD 项目结构说明

## 📁 目录结构

```
JYProgressHUD/
├── JYProgressHUD/                    # 主代码目录
│   ├── Core/                        # 核心文件
│   │   ├── JYProgressHUD.h          # Objective-C 桥接头文件
│   │   └── JYProgressHUDTypes.swift # 枚举和协议定义
│   ├── Views/                        # 视图组件
│   │   ├── JYBackgroundView.swift    # 背景视图
│   │   ├── JYRoundProgressView.swift # 圆形进度视图
│   │   ├── JYBarProgressView.swift   # 条形进度视图
│   │   └── JYProgressHUDRoundedButton.swift # 圆角按钮
│   ├── Extensions/                   # 扩展方法
│   │   └── UIView+JYProgressHUD.swift # UIView 扩展
│   ├── Resources/                    # 资源文件目录（预留）
│   └── JYProgressHUD.swift           # 主类文件
├── test/                             # 测试项目目录
│   ├── test/                         # 测试项目源码
│   └── README.md                     # 测试项目说明
├── JYProgressHUD.podspec            # CocoaPods 配置文件
├── README.md                         # 使用说明
├── LICENSE                           # Apache License 2.0 许可证
├── 集成说明.md                        # 集成指南
└── 项目结构说明.md                    # 本文档
```

## 📄 文件说明

### 核心文件

#### JYProgressHUDTypes.swift
- 定义所有枚举类型（Mode、Animation、BackgroundStyle）
- 定义委托协议
- 定义常量

#### JYProgressHUD.h
- Objective-C 桥接头文件
- 用于 Objective-C 项目集成

#### JYProgressHUD.swift
- 主类实现
- 包含所有核心功能：
  - 显示/隐藏逻辑
  - 动画系统
  - 布局管理
  - 进度更新
  - 时间控制

### 视图组件

#### JYBackgroundView.swift
- 背景视图实现
- 支持模糊和纯色两种样式
- 使用 UIVisualEffectView 实现模糊效果

#### JYRoundProgressView.swift
- 圆形进度视图
- 支持饼图和环形两种模式
- 使用 Core Graphics 绘制

#### JYBarProgressView.swift
- 水平条形进度视图
- 支持圆角效果
- 使用 Core Graphics 绘制

#### JYProgressHUDRoundedButton.swift
- 圆角按钮实现
- 支持高亮效果
- 自动调整圆角半径

### 扩展

#### UIView+JYProgressHUD.swift
- UIView 扩展方法
- 提供便捷的显示/隐藏方法
- 提供链式调用支持

## 🎯 核心功能

### 已实现功能

✅ **显示模式**
- 不确定进度（转圈）
- 确定进度（圆形饼图）
- 水平条形进度
- 环形进度
- 自定义视图
- 仅文本

✅ **动画效果**
- 淡入淡出
- 缩放动画（zoom in/out）

✅ **进度管理**
- 手动设置进度
- NSProgress 自动更新（使用 Combine）

✅ **时间控制**
- Grace Time（延迟显示）
- Min Show Time（最小显示时间）

✅ **布局系统**
- Auto Layout 约束
- 动态布局更新
- 支持偏移和边距

✅ **样式定制**
- 内容颜色
- 背景样式（模糊/纯色）
- 自定义视图

## 📦 安装方式

### CocoaPods

1. 在 `Podfile` 中添加：
```ruby
pod 'JYProgressHUD', '~> 1.0.0'
```

2. 运行：
```bash
pod install
```

### 手动集成

1. 将 `JYProgressHUD` 文件夹拖入项目
2. 确保所有 Swift 文件添加到 Target
3. 在需要使用的地方导入：
```swift
import JYProgressHUD
```

## 🔧 技术栈

- **语言**: Swift 5.9+
- **最低版本**: iOS 17.0+
- **框架**: UIKit, Foundation, CoreGraphics, Combine
- **架构**: MVVM + 协议导向

## ✅ 代码质量

- ✅ 无编译警告
- ✅ 无 Lint 错误
- ✅ 类型安全
- ✅ 线程安全（使用 @MainActor）
- ✅ 内存安全（使用 weak 引用）

## 📝 使用示例

### 基本使用

```swift
import JYProgressHUD

// 显示
let hud = JYProgressHUD.show(on: view)
hud.label.text = "Loading..."

// 隐藏
hud.hide(animated: true)
```

### 进度显示

```swift
let hud = JYProgressHUD.show(on: view)
hud.mode = .determinate
hud.progress = 0.5
```

### 链式调用

```swift
let hud = JYProgressHUD.show(on: view)
    .withMode(.determinate)
    .withLabel("Loading...")
    .withProgress(0.5)
```

## 🚀 下一步

1. 在项目中测试功能
2. 根据实际使用情况调整 API
3. 添加单元测试
4. 发布到 CocoaPods

---

**版本**: 1.0.0  
**创建日期**: 2025

